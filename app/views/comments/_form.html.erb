      <%= form_with(model: [post, post.comments.build]) do |f| %>
      <%= f.label "Insert comment" %>
      <%= f.text_field :content, class: "form-control", placeholder: "Insert your comment.." %>


      <%= f.submit "Post", class: "btn btn-primary mt-2" %>    
        <% end %>